{
  start: "2025-01-03",
  end: "2025-01-10",
  panel_name: "T1430",
  factor_time: "14:30",
  label_time: "14:42",
  refresh: true,
  overwrite: false,
  workers: 4,
  factor_workers: 1,
  compute: {
    backend: "auto",      // auto | torch | cpu
    torch_device: "cuda", // e.g. "cuda", "cuda:0"
    fallback_cpu: true,
    dataframe_backend: "pandas", // auto | cudf | pandas
    fallback_pandas: true,
    debug_log_each_record: true
  },
  factor_files: [
    "factor/lgbm_factor_MSE.json5"
  ],
  context: {
    stock_panel: { enabled: false, strict: false },
    bond_stock_map: { enabled: false, strict: false }
  },
  backtest_enabled: true,
  factors: [],
  backtest: {
    workers: 6,
    min_count: 10,
    ic_bins: 20,
    bin_count: 20,
    bin_select: null,
    bin_source: "auto",
    bin_top_k: 2,
    bin_lookback_days: 10
  },
  screening: {
    enabled: true,
    ic_metric: "rank_ic_mean",
    ir_metric: "rank_ic_ir",
    ic_abs_min: 0.015,
    ir_abs_min: 0.15,
    sharpe_min: 0.3,
    copy_reports: true
  }
}
