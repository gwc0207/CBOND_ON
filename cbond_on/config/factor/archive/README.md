# Factor Archive

Historical factor config snapshots were intentionally cleared during the
factor-config modularization pass. Keep this directory as a placeholder for
future manually approved archive drops only.
