{
  // One shared time window for all stages.
  start: "2025-12-01",
  end: "2026-03-20",

  // Choose model/strategy only. Detailed settings stay in model_score/backtest config.
  model_id: "lgbm_factor_ranker",
  strategy_id: "strategy01_topk_turnover",

  // Per-stage overwrite/refresh switches.
  panel: { refresh: false, overwrite: false },
  label: { refresh: false, overwrite: false },
  factor: { refresh: false, overwrite: false },
  model_score: { refresh: false, overwrite: false },
  backtest: { refresh: false, overwrite: false }
}
